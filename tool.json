{
  "name": "evaluate_fca_handbook_applicability",
  "version": "3.0.0",
  "description": "Evaluate which FCA Handbook entries apply to an entity, via the Metis FCA Handbook AI Harness. One-shot: no session, no conversation state. Supports quick mode (60-120 seconds, default) and full mode (longer, more detailed). Returns a compliance report with verbatim citations, gaps, and refinement suggestions. CRITICAL: This tool streams progress messages as it runs (one per reasoning node). You MUST display each message to the user as it arrives — do not wait silently. Silent waiting makes the user think the tool is broken. Actively incorporate the progress messages into your response, not just the final result.",

  "design_principles": {
    "single_input_field": "There is exactly one input field: user_input (max 5000 characters). No entity_description/question/context split. Put everything in one piece of text.",
    "literal_then_semantic": "The Harness's Node 2 semantically parses your one user_input blob into structured fields (entity type, the question, etc.) for its own internal pipeline. Be explicit about what you're asking — e.g. end with a clear question — since extraction quality depends on it.",
    "stateless": "Each request is independent. The calling LLM manages any multi-turn conversation; the Harness has no memory of previous calls.",
    "one_shot": "One complete analysis per request. To refine, reformulate user_input using refinement_suggestions and call again.",
    "no_hidden_state": "No session ID, no history, no background jobs. Each call is independent and self-contained."
  },

  "input_schema": {
    "type": "object",
    "properties": {
      "user_input": {
        "type": "string",
        "description": "Everything together, as one piece of text: firm type, regulatory status, products/services, key activities, target market, data handled, and the specific compliance question to answer. Be explicit about the question — e.g. end with 'Question: ...' — since extraction quality depends on it.",
        "maxLength": 5000,
        "required": true
      },
      "analysis_mode": {
        "type": "string",
        "enum": ["quick", "full"],
        "default": "quick",
        "description": "'quick' is a fast pass identifying the most critical binding obligations. 'full' performs detailed conditional reasoning — each entry's applicability, mandatory vs. conditional obligations, tensions between provisions, second-order gaps — and returns more refinement suggestions (2-4 vs. quick's 1). Full takes longer than quick's 60-120 seconds; use a ~300 second client timeout.",
        "required": false
      }
    },
    "required": ["user_input"]
  },

  "output_schema": {
    "analysis_content_schema": {
      "type": "object",
      "properties": {
        "summary": {
          "type": "string",
          "description": "2-3 sentence synthesis of what applies and why."
        },
        "entry_analysis": {
          "type": "array",
          "description": "One entry per retrieved FCA Handbook entry: {entry_id, entry_text, applies, conditional, reasoning}."
        },
        "obligations": {
          "type": "object",
          "description": "{high_confidence: [], conditional_obligations: [], low_confidence_or_tangential: []} — each an array of obligation strings."
        },
        "gaps": {
          "type": "array",
          "description": "Array of {gap, why_it_matters} — what the analysis couldn't resolve from your input."
        },
        "refinement_suggestions": {
          "type": "array",
          "description": "Array of {suggestion, priority}. Use these to decide what to ask next if refining."
        },
        "citations": {
          "type": "array",
          "description": "Array of {entry_id, cited_text, binding_level} — binding_level is R (Rule), G (Guidance), or E (Evidential)."
        },
        "tokens": {
          "type": "object",
          "description": "{input, output, total} — token counts for this run, for cost/complexity transparency. Not the billed amount."
        }
      }
    }
  },

  "calling_pattern": {
    "step_1_collect_input": "Gather all relevant context into one user_input string: the firm/product/service details plus the specific compliance question.",
    "step_2_call_tool": "Call evaluate_fca_handbook_applicability with user_input and optional analysis_mode.",
    "step_3_read_response": "Parse the response object. On success, read summary, entry_analysis, obligations, gaps, refinement_suggestions, citations, tokens.",
    "step_4_act_on_suggestions": "refinement_suggestions tells you what's worth asking next. If refining, weave the new detail into a fresh user_input and call again — a new, independent request.",
    "key_points": "The calling LLM owns the conversation. The Harness is one-shot and stateless. analysis_mode defaults to 'quick'; ask the user if 'full' seems warranted (they're weighing a significant decision, or quick's summary left real gaps) rather than defaulting to it — it takes longer."
  },

  "key_insights": [
    "user_input is required; analysis_mode ('quick' or 'full', default 'quick') is optional. No entity_description/question/context split.",
    "Be explicit about the question inside user_input — a Haiku call semantically extracts it, and vague phrasing produces worse retrieval.",
    "refinement_suggestions tell you what to ask next; you decide whether to refine or conclude.",
    "tokens is for cost/complexity transparency only — billing is a flat £7.95 per call regardless of token count.",
    "Stateless, one-shot: no session ID, no history. Each call is independent.",
    "Verbatim citations from the FCA Handbook, not generative invention — but entry_analysis/citations can legitimately come back empty if retrieval didn't find a strong match; that's not necessarily an error."
  ]
}
