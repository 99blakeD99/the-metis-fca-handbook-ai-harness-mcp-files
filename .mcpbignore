# Build artifacts
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Manifests for other distribution channels (PyPI, MCP registry) — not needed in the bundle
requirements.txt
server.json
server.json.backup

# Not relevant to the bundle
.gitignore

# OS
.DS_Store
